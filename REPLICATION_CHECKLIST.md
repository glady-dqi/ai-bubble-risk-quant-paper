1. Crear entorno: `python3 -m venv .venv && source .venv/bin/activate`.
2. Instalar dependencias: `pip install -r requirements.txt`.
3. Descargar datos: `python scripts/fetch_data.py`.
4. Ejecutar pipeline: `python scripts/run_pipeline.py`.
5. Ver resultados en `results/`.
6. Ver figuras en `figures/`.
7. Compilar paper: `pdflatex paper/main.tex`.
8. Revisar `EXECUTIVE_SUMMARY.txt`.
9. Ver `TASKS.md` para progreso.
10. Reproducir parámetros LPPL en `results/lppl_params.csv`.
